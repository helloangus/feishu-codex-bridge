# 开发计划与交接

本机打包验收（2026-09-13）：`bash package-rust.sh` 完成 release 构建，产物为 `dist/feishu-codex-bridge.Y9e5Wowx`，二进制约 12 MiB。五项 SHA256 校验全部通过；发布目录二进制的 help、临时目录配置生成/校验、离线 service status 通过，配置权限 0600、重复生成拒绝覆盖、查询不创建状态目录均确认。环境未安装 file 命令，未进行该项文件类型检查；二进制已在本机实际执行成功。未启动服务、上传发布或替换运行入口。产物包含当前未提交工作区代码，不代表正式版本发布。CLI help 仍保留旧“生产机器人由 start.sh 管理”描述，作为文案遗留记录。

用户范围确认：Termux 重启后不要求自动恢复，移出开发待办；打包已完成本机验收。下一步仅按需继续实机故障恢复及长时间稳定性验证。

最新开发暂停点：首次部署与交付入口已补齐，等待切换模型统一验证。新增 `bridge config init`，以受限白名单/配对和 workspaceWrite 为默认，生成权限 0600 的无凭据 TOML，拒绝覆盖已有文件；新增 setup-rust.sh 配置引导、start-rust.sh 启停包装、package-rust.sh 本机 release 目录与 SHA256SUMS。更新 Rust 部署、开发及架构入口。新增配置生成和脚本回归用例；本批未运行格式化、编译、测试、打包或真实服务。

本批统一验证结果：`cargo test --workspace --locked -- --test-threads=1`、`cargo clippy --workspace --all-targets --locked -- -D warnings`、`cargo build -p bridge-cli --locked`、`cargo doc --workspace --no-deps --locked`、`cargo fmt --all -- --check`、`cargo xtask check-boundaries`、Python 回归和 Python 语法检查均通过；Python 共 62 项通过，2 项 schema 用例因缺少可选依赖跳过。shell 语法和 `git diff --check` 通过。未运行 release 打包命令，未启动真实服务。

真实飞书验收记录（2026-09-13）：使用现有白名单配置和 Rust 原生入口启动 `guard → supervise → run`，原生 WebSocket 成功建立，连续心跳保持新鲜。用户已完成 `/status`、`/help`、普通模式文本、Plan 模式必答问答、答案回传、附件输入、文件修改成果物回传、停止/新会话/状态等验收操作。运行日志记录 QuestionReceived、QuestionSent、AnswerReturned、7 轮 TaskStarted/TaskFinished、FilesPrepared/FilesFinished 和 AnswerDelivered；状态为 connected/running，未发生监督重启或退避。未执行 release 打包。尚未覆盖设备重启自启动、外层守护强杀后的系统级接管和 72 小时持续运行。

Termux 重启验收记录（2026-09-13）：重启后检查发现 guard、supervisor 和 bridge 均未自动启动，运行锁已释放；旧 `health.json` 保留最后的 connected 快照，但 status 正确报告 `running=false`、`heartbeat_fresh=false`，不能误判为在线。没有新的启动/连接事件。设备重启自启动仍是待开发/部署配置项，当前服务恢复能力只覆盖进程运行期间的内层异常退出。

下一步：统一运行 Rust 全 workspace 测试、fmt、Clippy、构建、依赖边界、Python 回归、shell 语法与空白检查，再检查打包产物。较大验证问题只记录。真实飞书交互、目标平台两层进程接管、72 小时稳定性及生产回退演练仍待验收，不能将开发完成视为生产验收通过。

记录修正：上一批可确认的是 bridge-cli 测试、构建、Clippy 与 Python 56 项通过、2 项跳过；下文上一批“全 workspace 测试已通过”的概括不作为全量验证证据。本次统一验证须补齐。

以下为历史批次记录，其中“下一步”和“待开发”描述以本文顶部最新状态为准。

当前整批开发暂停点（用户配对与恢复增强，连同权限审批已完成离线验证）：Rust 支持 /pair <配对码>，从 pairing_code_env 读取 16–256 字节无空白的私密值。先持久化 allowed_open_ids 再更新运行时授权，配对码不进入任务/状态/日志；错误尝试不落盘，全局每分钟最多 10 次，重启重置窗口，超过限额静默接受事件而不回复。保存失败不授权，允许重试；未配置配对码仍按白名单工作。已补持久化/失败/新用户授权后命令访问回归，相关测试与静态检查通过。

服务恢复开发：新增 bridge guard --config 外层 Rust 守护，service start 默认经 guard → supervise → run；两层独立锁、socket、快照和结构化日志。外层在内层死亡后清理接管的后代，成功才重启，最多 10 次重试且不因内层运行时间重置；正常退出不重启。guard 的快照/日志位于 <state>/guard/runtime/supervisor，锁/socket 位于 <state>/runtime/guard.lock 与 guard.sock，status 增加 guard_running/guard_snapshot，supervisor_running 表示任一监督层持锁。控制请求优先送外层，stop 最长等待 60 秒；内层停桥接宽限 20 秒，外层停内层宽限 45 秒。外层自身 SIGKILL 后仍需终端/操作系统接管。新增控制、锁和恢复回归，已通过本轮验证。

心跳恢复开发：每 5 秒观测所属子进程的健康快照，单调时间判定进展，启动宽限 120 秒、已有心跳 45 秒不更新触发停止/清理，之后等待 30 秒再重启；次数有界，不重放任务。快照读取使用阻塞池、2 秒超时；健康文件拒绝链接及非普通文件，避免 FIFO 阻塞。进程心跳不证明每个业务请求健康。已补心跳时限、外层锁和控制通道回归，并通过本轮构建、测试与静态检查；两层真实进程接管与 Termux/proot 长时间行为需人工验证。

本次已完成统一验证：Rust workspace 测试、bridge-cli 构建、全 workspace Clippy、fmt、依赖边界、空白检查和 Python 58 项回归通过，2 项 schema 测试因可选依赖缺失跳过；未启动/重启真实服务。下一步继续 Rust 首次部署引导与文档收尾，之后进行真实飞书和 Termux/proot 集成验收。

当前开发暂停点（额外权限与网络审批一起完成开发，待切模型验证）：additionalPermissions 解析 network.enabled、fileSystem 的 read/write/entries/globScanMaxDepth，覆盖路径、glob、root/minimal/project_roots/tmpdir/slash_tmp 与带子路径的特殊集合；networkApprovalContext 展示主机及 HTTP/HTTPS/SOCKS5 TCP/UDP。适配层严格解析后转换为完整文字，未知字段/枚举/畸形类型拒绝协议请求；特殊路径 unknown 展示但只能拒绝，超出展示上限不能同意。纯网络请求允许缺少命令/目录；包含额外权限的命令仍需完整命令与目录。只回传 accept/decline，遵循 availableDecisions，不保存会话批准、execpolicy 或网络规则。沿用原卡片归属/一次性/超时与关闭拒绝机制。新增解析、回传、RPC 卡片交互回归和两个版本协议样例，未运行格式化、编译、测试或静态检查，未操作真实服务。

下一步：统一验证本批解析、完整展示、仅本次回传、未知与超长详情、跨用户/重复点击及既有审批回归；通过后继续服务生命周期集成收尾。远端执行环境与 grantRoot 会话目录授权仍仅拒绝，不计入本次两个字段的支持范围。

当前开发暂停点（归档通知同步及启动修复已完成离线验证）：在上一批运行中通知同步之上，生产 bootstrap 在飞书接收业务前读取所有本地绑定 ID，查询 Codex 完整归档列表并一次性提交匹配绑定的清理。查询覆盖版本 schema 的全部来源及模型提供方，最多 100 页、每页 100 项，总核对时限 60 秒；重复游标、缺失/错误游标、畸形 ID、分页失败或超限均拒绝启动，不发布部分扫描结果。空绑定不查询，未被正面确认归档的绑定保留，偏好设置保留；清理幂等，不重放远端归档、取消归档或业务任务。启动失败先关闭 Codex 子进程。这样可在后续重启时修复漏通知和远端归档成功后的本地清理失败；持久化故障本身仍须排除。

本批验证范围：调度门控、运行中通知、跨用户/目录绑定清理、旧 epoch、分页/坏响应/远端错误、原子保存失败及重开后的状态。新增 archive_reconciliation.rs 三项测试，与 session_reset.rs 和 scheduler 回归一起通过；Rust workspace 测试、构建、Clippy、fmt、依赖边界和空白检查通过，Python 58 项回归通过，2 项 schema 测试因缺少可选依赖跳过。未操作真实服务。限制：归档列表分页不提供跨客户端事务快照，只采信查询时观察到的归档；不主动取消当前执行，外部通知广播能力仍须真实验收。下一步进入权限审批补全阶段。

上一批开发（归档通知同步）：主循环消费当前连接 epoch 的 thread/archived 通知，按会话 ID 合并最多 128 项待同步目标。等待当前执行和会话变更结束后，在下一项任务准备前清除该 thread 的全部本地绑定；不向 Codex 重发归档，不自动切换或恢复会话。同步期间沿用会话变更门控，写盘失败停止运行；收到通知和实际同步时撤销待实施计划。该批已通过离线回归和静态检查，未操作真实服务。

本批边界：只处理本连接实际收到的通知，不保证 app-server 广播其他客户端的操作；断线/进程退出期间丢失通知由启动时归档核对补偿。当前任务允许结束后再同步；待同步集合仅在内存中，不能替代跨系统部分失败自动协调。

当前开发暂停点（监督阶段持久化与日志已完成离线验证）：新增 `runtime/supervisor/snapshot.json` 与独立 `runtime/supervisor/events.jsonl`，记录监督器 PID、时间、starting/running/backoff/stopping/stopped/failed 及本次退避秒数，日志复用 2 MiB/3 份轮转。状态查询新增 supervisor_snapshot，退出后仍可读取；必须结合 supervisor_running，历史 running 不表示在线。发布使用同步与原子替换，拒绝临时文件符号链接/硬链接。初始化与状态/日志写入失败返回错误；子进程已启动时先请求停止并清理，提前返回尽力记录 failed。正常停止发布 stopped 后释放监督锁；控制 stop 若读到非 stopped 终态拒绝继续 restart。新增快照/日志保留、正常与提前失败终态、链接拒绝回归。Rust workspace 测试、构建、Clippy、fmt、依赖边界和空白检查通过；Python 58 项回归通过，2 项因缺少可选 schema 依赖跳过。未操作真实服务。

本批范围：后台普通 stderr 仍丢弃，新增日志仅记录结构化监督阶段，不记录原始错误文本或凭据。强杀/abort/断电可能保留旧快照；阶段和日志非同一事务，写入失败可能只更新其中一项。完整孤儿接管验收仍待完成。下一步继续后台启停与异常退出的离线集成场景；较大验证问题只记录。

最新开发（PRoot 兼容修复）：用户确认运行环境为 Termux 内的 proot Ubuntu。诊断确认子进程 status/PPid 正确，但 `/proc/<父PID>/task/<父PID>/children` 不存在。原实现忽略 NotFound 会把无法枚举误判为无后代；本批在 children 文件缺失时回退枚举 `/proc/<pid>/status` 的 PPid，并要求自身条目可见，权限错误等不静默忽略。恢复独立测试进程中实际子进程清理用例，新增临时 proc 数据的父子筛选回归。

PRoot 兼容批次验证：`bridge-cli` 17 项单元/集成回归通过，其中包含实际 `sleep` 子进程的 pidfd 清理；Rust 全工作区串行测试、构建、Clippy（`-D warnings`）、fmt、依赖边界和空白检查通过。Python 回归 58 项运行，56 项通过，2 项缺少可选 schema 依赖而跳过。之前的孤儿接管场景因 proot 测试进程观察限制无法稳定复现，已改为稳定的实际子进程清理测试并记录；目标服务异常退出后的孤儿接管、Termux 权限和长时间运行仍待人工验证。未启动或重启真实服务。下一项开发为监督阶段持久化与后台日志；验证阶段遇到较大问题只记录，不扩大修复范围。

最新开发（后台服务控制）：本批新增 `bridge service --config <文件> start|stop|restart|status`，本地 Unix socket 接收状态/停止，控制锁串行化后台变更命令，启动以本次子进程 PID 的 socket 响应确认监督器就绪，停止等待监督锁释放后才允许重启。监督阶段在内存中展示 starting/running/backoff/stopping，不代表飞书在线，backoff 数字为本次计划等待秒数而非实时倒计时。Linux 监督器启动前探测 pidfd、/proc 与 subreaper，桥接退出后接管遗留后代，通过 pidfd 发信号并回收，5 秒未清理完则停止自动重启。控制通道错误取消运行。新增控制锁、socket 状态/停止和无后代清理回归；真实后代接管仍需目标 Linux/Termux 集成验证。

服务控制批次验证：Rust 全工作区串行测试、`bridge-cli` 构建、全 workspace Clippy（`-D warnings`）、fmt、依赖边界及空白检查通过。验证中发现依赖 Cargo 测试环境的 `/proc/self/task/*/children` 无法稳定观察新建子进程，已移除该不可靠测试并记录；没有因此修改清理实现。Python 回归 58 项运行，56 项通过，2 项缺少可选 schema 依赖而跳过。未启动或重启真实服务。下一项开发为监督阶段持久化与后台日志，再进行目标平台集成验证。

本批限制与后续：监督器自身被 SIGKILL 后仍无更外层接管保证；阶段尚未持久化，后台 stderr 当前丢弃，子进程结构化日志仍由原日志模块落盘；启动超时无法联系本次实例时报告错误并保留进程供状态排查。Termux 必须验证 pidfd/prctl 支持，探测失败拒绝监督启动。后台启停进程集成、失败清理及平台兼容仍待人工验证，再完善持久化监督快照和监督日志。

最新开发（监督器状态查询）：`bridge status` 新增独立 `supervisor_running` 字段，通过监督锁查询，不创建文件、不依赖 PID；即使子进程从未启动或处于退避阶段，也能显示监督器锁被占用。原 `running` 仍只表示桥接运行锁。监督锁读写路径增加非阻塞打开及普通文件/硬链接校验。锁占用只能说明实例持有锁，不能证明监督器健康；两把锁分开观测，不保证原子一致。

监督器状态批次验证：新增离线回归覆盖没有子进程时的监督状态、锁释放后残留文件不代表运行，以及符号链接/硬链接拒绝；全 workspace 串行测试、`bridge-cli` 构建、全 workspace Clippy（`-D warnings`）、fmt、依赖边界及空白检查通过。Python 离线回归 58 项运行，56 项通过，2 项缺少可选 schema 依赖而跳过。未启动或重启真实服务。下一项开发为后台启停、监督阶段快照与 Codex 后代生命周期。

最新开发（Rust 前台监督器）：新增 `bridge supervise --config` 前台监督入口和独立 `supervisor.lock`，每次启动独立 `bridge run` 进程，正常退出不重启；异常退出按 2/4/8/16/30 秒退避，连续短时失败最多重试 10 次，运行满 60 秒重置预算。停止信号可中断退避，运行中先转发 TERM，20 秒未退出则强制结束直属子进程并报错，不再重启。不会依据状态文件 PID 发信号。后台 start/stop/restart、监督器状态发布以及异常死亡后的 Codex 后代清理仍待开发；强杀后代回收未完成前不将此入口作为完整生产 supervisor。

监督器批次验证：新增退避预算与预取消不启动子进程回归；全 workspace 串行测试、`bridge-cli` 构建、全 workspace Clippy（`-D warnings`）、fmt、依赖边界及空白检查通过。Python 离线回归 58 项运行，56 项通过，2 项缺少可选 schema 依赖而跳过。未启动或重启真实服务。下一项开发为后台启动、停止、重启、状态发布及 Codex 后代进程生命周期。

最新开发（Rust 周期健康心跳）：主循环启动后立即发布一次心跳，之后每 10 秒更新，跳过错过的 tick；取消时结束，退出前等待心跳任务结束再发布终态。心跳写入失败取消本次运行，退出结果计为失败。`bridge status` 新增 `heartbeat_fresh`：30 秒内为 true，过期或已退出为 false，旧快照缺字段或时钟倒退为 null；连接状态独立保留。它只能观测心跳调度及写入，不证明所有业务任务和网络健康。

心跳批次验证：新增 3 项离线回归覆盖新鲜度、终态保护、取消和写入失败；全 workspace 串行测试、`bridge-cli` 构建、全 workspace Clippy（`-D warnings`）、fmt、依赖边界和空白检查通过。Python 离线回归 58 项运行，56 项通过，2 项缺少可选 schema 依赖而跳过。未启动或重启真实服务。下一项开发为后台启动、停止、重启及异常退出退避监督。

最新开发（Rust 持久化诊断日志）：`bridge run` 在获得运行锁后安装受限的诊断日志接收器，写入 `<state_dir>/runtime/events.jsonl`，单文件 2 MiB 轮转并保留 3 份，权限为 0600。它只接收诊断模块产生的元数据，拒绝活动日志的符号链接、硬链接和非普通文件；初始化失败拒绝启动，运行中写入失败只输出固定 stderr 事件，不重放业务。新增启动及飞书连接、重连诊断事件；panic 路径采用非阻塞锁，无法写入时回退 stderr，避免递归等待。

日志批次验证：新增 2 项离线回归覆盖轮转保留、重开追加、权限、超长/换行记录拒绝、符号链接拒绝和 panic 时锁占用；全 workspace 串行测试通过，`bridge-cli` 构建、全 workspace Clippy（`-D warnings`）、fmt、依赖边界和空白检查通过。未启动或重启真实服务。下一项开发为周期健康心跳与后台启动、停止、重启及退出退避监督。

最新开发（2026-09-11，Rust 服务状态）：`bridge run` 在 App ID 全局锁后持有独立运行状态锁，原子发布启动、飞书连接/重连及正常/异常退出状态到 `<state_dir>/runtime/health.json`。新增 `bridge status --config` 只读查询，不需要凭据；运行锁已释放时旧 connected 快照不代表存活。健康发布失败停止运行。新增离线测试覆盖并发实例拒绝、正常/异常退出、残留快照、损坏文件和临时文件符号链接拒绝。本批不启动或重启真实机器人。

下一项：接入周期健康心跳、后台启动/停止/重启及退出退避监督。当前状态查询仅报告锁占用和最后状态，不判断持锁进程是否卡死；完整 P5 尚未完成。

服务状态批次验证：3 项离线健康状态测试通过，CLI 构建及 `status --help` 验证通过，Clippy（bridge-cli 所有 target，-D warnings）、fmt、依赖边界和空白检查通过。未连接真实飞书或重启服务。

最新修复（协议问答必答）：仅匹配当前 Plan 执行身份的 `item/tool/requestUserInput` 请求进入问答卡片，普通 Output 中的选项文本不触发问答。移除空答案继续路径：超时、任务结束/停止前未答完、卡片交付失败、重复/过量请求、错误模式/身份及无法完整展示的题目，停止本次运行并中断当前任务，不回传空或部分答案。正常关闭也不替用户补答案；只有每题均有非空回答才回传。完整服务自动恢复尚待开发，本批不重启验收服务。

最新开发（验收诊断）：新增时间戳、PID 和内部任务编号的结构化日志，覆盖任务准备/启动/结束、附件准备、问答接收/发送/答案回传、最终回复送达、逐项成果发送与整理结束。重连记录错误类别，心跳超时有独立事件；CLI 记录正常/异常返回与 panic 发生，不输出 panic 载荷。日志写入 stderr，持久化、轮转及完整监督仍待开发。本批尚未重新启动服务。

诊断批次验证：全 workspace Clippy（-D warnings）、fmt、依赖边界及空白检查通过。全量串行测试在 WebSocket ACK 超时测试出现一次 Elapsed 后停止；单独复跑该 WebSocket 套件 8 项全部通过，另补跑 bridge-local 全部测试通过。保留该偶发超时记录，不宣称本轮全量一次通过。新增日志测试验证内部任务编号保留、任意文本与换行注入被丢弃。

验收记录更正：`.runtime/rust-minimal.log` 最后修改于 2026-09-08 13:36（本机时间），其中的 I/O driver panic 不能证明 9 月 9 日验收发生相同故障；现有证据也不能证明存在自动监督重启，历史记录无时间戳，不能计算重连频率。根因待新日志和复现证据确认。

- [x] 已修复问答超时/发送失败回传空或部分答案继续执行，以及超限题目错误提示自行回答的路径。异常采用停止本次桥接运行，待重新启动后由用户重新发起任务；真实飞书复验待进行。

当前开发检查点：Rust 原生飞书长连接已接入 bootstrap，当前工作区的 `bridge run` 不再启动 Python SDK；旧 python/adapter 配置字段兼容读取但不使用。新增 `bridge-feishu/src/websocket/{mod,wire}.rs`、`proxy.rs`：地址获取、pbbp2 Protobuf、EVENT/CARD、分片、应用确认后 ACK、心跳/Pong 超时、重连，以及 HTTP/HTTPS CONNECT、SOCKS5/5h 和 no_proxy 决策。依赖和样例配置已更新；兼容 sidecar 源码保留，未部署。

原生连接离线专项覆盖：8 项 WebSocket、5 项重连/配置、3 项代理、2 项 TLS 和 1 项 CLI 配置测试。通过内存连接验证分片、应用确认与失败 ACK、心跳、容量与取消、重试预算与连接替换；模拟地址发现验证重连使用新端点。代理覆盖认证、绕过规则及 IPv6；TLS 覆盖直连和 HTTP/HTTPS 隧道，以及不受信任证书和错误主机名拒绝。修复 Pong 部分配置覆盖重试策略、重复空 device_id、IPv6 带端口绕过规则及代理 5xx 重试分类。真实直连/代理、断网恢复和 Termux/手机验收继续延后，P4 尚未全部验收。下一步开发 Rust 服务生命周期与运维入口。

协议依据：本地 SDK 的 `lark_oapi/ws/client.py`、`model.py`、`const.py`、`pb/pbbp2_pb2.py`，以及[官方 SDK 源码](https://github.com/larksuite/oapi-sdk-python/blob/v1.7.3/lark_oapi/ws/client.py)。离线验证不代表真实飞书或 Termux 验收完成。

本轮验证：`cargo test --workspace --locked -- --test-threads=1` 共 169 项通过；Python 回归运行 58 项，56 项通过、2 项因缺少可选 schema 测试依赖跳过。fmt、Clippy（所有 target，禁止警告）、依赖边界与 diff 空白检查通过。未重启服务、部署或进行真实飞书验收。

真实验收补充：只有 Plan 模式支持 Codex 问答；普通执行模式收到问答请求会停止本次运行，不回传空答案。Plan 模式必须通过选项或“其他／自行回答”完成，不能展示或接受“跳过本题”。此前问答卡片把跳过作为通用入口，已记录为验收 bug；代码已改为关闭跳过入口并限制模式，待重编译、重启后复验。

日志验收补充：`.runtime/rust-minimal.log` 能确认原生连接建立及多次自动重连，但当前 Rust 运行日志没有记录消息、问答、附件或成果物的业务结果，不能单凭日志复盘这些验收项。历史运行还出现 Tokio I/O driver `Function not implemented` panic，需在后续服务生命周期开发中补充 panic 捕获、退出原因和业务事件日志，确认外层监督不会掩盖异常。

最新开发（回答展示）：Rust 最终 Markdown 回复卡片、UTF-8/代码块分段、段号与失败文字降级已接通；运行中最多每 3 秒更新原进度卡，预览 1,000 字符、最多一个待发送进度，失败停止本轮刷新。进度与完整回复串行，结束先关闭预览，再发送完整结果；退出发送等待仍有界，不自动重放。验证记录见本批交接，未部署或真实飞书验收。

回答展示批次验证：Rust workspace 串行全量 150 项通过；新增 5 项展示回归并扩展真实 RPC/附件主循环测试，覆盖中文超长代码行、未闭合/波浪线代码块、同卡片更新到终态、预览失败停止重试、最终多段和文字降级，以及成功/失败颜色。全 workspace Clippy（`-D warnings`）、fmt、依赖边界及空白检查通过。本批未修改 Python、未重跑 Python 回归，未部署或重启服务。下一项开发为完整 supervisor 健康状态与异常退出监督，真实飞书展示验收继续延后。

最新开发：Rust Plan 结果确认卡片已接入，提供原会话实施、清空上下文后实施、继续讨论三项一次性选择。仅成功 Plan 轮次的完整计划（最多 16,000 字节）开放，等待成果交付结束；绑定用户/聊天/目录/会话，10 分钟失效，新任务及状态修改撤销旧按钮。先耐久认领，再校验原会话与模式、保存设置并入队；失败不自动重试，部分保存会明确提示。真实飞书验收和部署继续延后。

Plan 批次验证：Rust workspace 串行全量 145 项通过（新增 7 项 Plan 集成回归），全 workspace Clippy（`-D warnings`）、fmt、依赖边界与空白检查通过。覆盖三种选择及默认模式 RPC、原/新会话区别、身份/来源/超时/会话变化拒绝、重复操作、旧卡片更新、认领保存失败、发送失败及提前完成事件。本批未修改 Python、未重跑 Python 回归，前批结果不计为本批验证。下一项为最终回答 Markdown 卡片与流式进度。

当前进展（2026-09-09）：问答、附件与成果物开发已接入同一 Rust 主循环，以下当前清单取代后文历史检查点的“自由文本/附件未迁移”描述。最终验证结果见本批交接记录；未部署，真实飞书验收延后。

本批验证：Rust workspace 串行全量 135 项通过；随后新增 3 项文件回归并重跑 bridge-local 全部 24 项通过（累计 138 项）。全 workspace Clippy（`-D warnings`）、fmt、依赖边界、Python 语法及空白检查通过。Python 58 项中 56 通过、2 项缺 `requirements-test.txt` 中 schema 依赖而跳过。新增测试覆盖答案身份/重复/大小/多行/跳过、富文本附件解析、授权与去重后下载、图片进入真实 RPC 参数、交付门禁、生成图片线程隔离与内容去重、下载失败清理、文本差异及上传失败汇总。无真实网络、机器人验收或部署；既有默认并发 Locked 问题未在本批宣称修复。

- [x] 问答：仅 Plan 模式支持单选及其他/无选项/敏感题自行回答；普通执行模式或异常请求停止本次运行且不回传空答案；Plan 模式不展示或接受跳过；多行专用答案命令、用户/聊天/目录/任务/题号/时限校验，重复答案拒绝；只有所有题目均得到非空回答才完成一次回传。
- [x] 附件：文本与富文本资源解析、授权后按任务下载、私有暂存与 no-follow 路径、图片头校验、每项 20 MiB/每条 10 项限制、失败不提交半成品，图片输入传给 Codex。
- [x] 成果：任务前后有界快照、工程文本差异、已知成果上传、当前线程生成图片、SHA-256 内容去重、过滤后统一 10 项限制、失败/超限汇总；交付完成前不启动下一任务。
- [ ] 本批真实飞书问答、附件下载、卡片显示及上传验收；按用户要求延后，未重启或部署服务。
- [x] Plan 结果确认/实施卡片与任务绑定，含清空上下文实施及继续讨论。
- [x] 最终回答 Markdown 卡片与流式进度、长回复分段及失败降级。
- [ ] 下一项开发：完整 supervisor 的健康状态、异常退出监督与服务生命周期；随后核对迁移剩余项。P3 不因本批功能完成而标记全部完成。

本批文件审批检查：bridge-app/bridge-codex 的 40 项相关测试及 7 项审批运行时集成测试通过；全 workspace Clippy（-D warnings）、fmt、依赖边界和空白检查通过。本批没有重复运行全 workspace 测试或 Python 回归，不将上批结果计为本批全量验证。

最新补充（文件审批）：逐文件差异已接入。先到达的 fileChange item/started 按连接/线程/轮次/条目精确关联，展示路径、add/delete/update、movePath 和完整 diff。缓存最多 32 项，每项 20 文件/16 KiB；单字段 4 KiB、卡片 24 KiB 展示上限。缺失/晚到/超限继续拒绝，重复快照撤销同条目待审批；grantRoot 会话级授权仅拒绝。下方“文件修改仍缺逐文件差异”的历史检查点由此条更新。下一步开发问答卡片与逐题答案回传；本批未部署、真实飞书验收继续延后。

最新检查点：命令审批卡片已接入 Rust 运行时，支持一次性同意/拒绝回传、任务/用户/聊天/目录/原消息校验及 10 分钟超时拒绝。启动响应前请求先等待身份确认；发送失败、停止、结束时拒绝，关闭时限时尝试拒绝；回传结果不确定时停止且不重试。最多保留 32 项待审批。文件修改仍缺逐文件差异，远端执行环境及不能完整展示的请求只提供拒绝；权限 overlay 仍按协议不兼容处理。问答仍返回空答案。下一步补文件修改详情，再接入问答；真实飞书验收继续延后。

本批审批验证：5 项运行时集成测试经过真实本地 RPC 回复适配器，覆盖同意/拒绝、用户/聊天/来源拒绝、文本审批拒绝、重复点击、发送失败、虚拟时钟超时、停止/完成/关闭、启动响应前等待及回传失败。Rust workspace 串行全量、Clippy（-D warnings）、fmt、依赖边界、语法与空白检查通过；Python 58 项中 56 通过、2 项缺 schema 依赖跳过。未部署，未执行真实飞书验收。既有并发 Locked 问题未在本批修复。

继续开发时先阅读本文件、README.md 和 git log，并核对实际运行状态。
每完成一项同步更新计划；不要把语法检查当作飞书端联调通过。
用户当前安排：优先功能开发，真实飞书验收延后；每项开发交接都说明已完成、尚未完成和下一步具体功能。

## 已实现

- [x] 飞书私聊接入 Codex，文本文件保存会话 ID，恢复会话、切换模型、停止、压缩。
- [x] 图片/文件输入与交付物上传。
- [x] JSON 2.0 卡片、按钮回调、审批详情与原卡片更新。
- [x] 任务耗时更新、长回复按 Markdown/代码块安全分段、附件上传结果列表。
- [x] /help 常用命令按钮：状态、恢复、新建、模型、压缩、停止。
  点击直接执行命令，不冒充用户另发聊天消息；上线后需要飞书实际点击验收。

## 下一阶段：后台稳定运行（用户已同意）

进展：已增加 service.py 管理入口、同项目 flock、日志轮转与认证字段过滤。
会话恢复已用独立真实 Codex app-server 对已保存映射完成无副作用恢复检查；飞书端重启后下一条正常提问可顺带完成端到端确认。
已增加同一个 FEISHU_APP_ID 的全局锁，多个项目目录不会同时运行同一机器人。
已实现单一 RPC 读取分发、真实请求超时、断连接唤醒，以及 Codex 重启后的会话映射清理。
独立真实 Codex 进程验证：8 个并发查询、杀进程后重启查询通过。

- [x] 服务管理：启动、停止、重启、状态；使用目录锁和机器人全局锁防止重复运行。
  兼容 Termux，不依赖 systemd；保留前台调试方式。PID 必须验证归属再停止。
- [x] 日志落盘：自动轮转，记录结构化事件和错误类型；过滤机器人密钥、token、WebSocket URL 的认证参数。
  不默认记录聊天全文、审批命令内容或文件内容。
- [x] 服务健康状态：`./start.sh status` 读取无敏感字段的状态文件，显示桥接启动、就绪、飞书连接与自动重启状态。
  已实际重启并确认状态从“已初始化”变为“已连接飞书”。
- [x] 自动化回归测试：覆盖会话/模型持久化、卡片移动端布局、长代码块分段、状态卡与服务健康状态。
- [x] 异常恢复：监督器检测桥接异常退出后按 2/4/8…30 秒退避重启；正常 stop 不重启。
  飞书 SDK 自带断线重连，Codex 退出后桥接会保留会话文件供下一条消息恢复；失败任务有明确终态。
  不自动重复执行可能已产生文件修改的任务。
- [x] 修复并发 RPC 读取：当前命令线程与任务 worker 可能同时读取 app-server stdout。
  改为单一读取分发并匹配请求 ID，防止 /resume、/models、/compact 与任务互相抢响应。
- [x] 验收：重复启动、异常退出、服务重启后的飞书重新建连、日志脱敏；保存的会话映射可由全新 Codex app-server 恢复。

## 后续体验完善

- [x] 统一卡片分区：对话底部导航独立标题与分隔线，选择题独立选项及“自行回答”分开，控制面板各组与停止按钮明确分隔；同一条目操作保持相邻。已补布局回归，手机端视觉验收待确认。

- [x] 对话归档与取消归档：列表显示标题、完整 ID 和当前对话标记；支持原卡刷新、归属/过期校验及任务入队互斥，归档清理绑定。接口按本机 Codex CLI 0.153.4 schema 核对。
- [x] 工程文本只展示本轮差异；已知图片/文档等成果才上传；缓存、依赖与编译产物不读取、不比较、不上传。结束快照复用，附件过滤后统一限额；修复超长单行代码块分段循环。
- [ ] 上述两项真实飞书验收及手机卡片截图：已通过 44 项离线回归、语法/空白检查和 setup 前置检查，并通过 start.sh restart 加载新代码，status 已确认重新连接飞书；仍需按 docs/operations.md 做手机端人工验收，离线回归不代表联调通过。

- [x] Plan 模式与选择题交互：`/plan` 按用户/目录保存，计划完成卡支持实现、清空上下文后实现和继续规划；Codex 选择题以飞书选项卡和“其他”文字回答回传。
- [x] 分离 Plan 详情、过程与文件差异：`/help` Plan 为状态型原卡开关；完成卡与完整计划详情分开；受限文本快照生成本轮 unified diff 卡，交付物仅列文件。
  已补离线回归；仍需真实飞书点击开关、查看 Markdown 计划详情及新增/修改/删除 diff 卡。
- [x] Plan 操作置底：原进度卡只显示完成状态，计划详情发送完成后再发送带三项操作的“计划下一步”卡，避免用户回到上方点击。
- [x] Turn sandbox 与审批：每轮固定为当前工作目录的 `workspaceWrite`，网络关闭，审批策略为 Codex Ask for approval（`on-request`）；常规目录内读取不再因桥接遗漏策略而落入本机默认规则。
- [x] Termux sandbox 兼容：`CODEX_SANDBOX_MODE` 默认 `workspaceWrite`，setup 首次配置明确询问是否选择 `dangerFullAccess`；仅显式配置时关闭 sandbox。
- [x] 交互时限提示：审批、Codex 选择题和 Plan 后续操作卡显示时限及超时后果；超时更新原卡为终态。

- [x] 文档体系：重写面向 GitHub 访客的 README，并新增架构、设计、开发、部署和运行手册，记录实际边界、线程模型、协议、状态、运维和扩展约束。
- [x] 可移植部署：新增 `setup.sh` 交互引导，自动安装 Python 依赖、检查 Codex、生成私有 `.env`、设置工作目录并启动服务；README 保留完整飞书后台和 Codex 登录人工步骤。
- [x] 消息重投防护：最近 1,000 个飞书消息 ID 持久化且加锁去重；重启后不会重复执行已确认事件，日志以短哈希关联事件而不记录内容。
  已在重启后飞书端验证 `/help` 和状态按钮各只处理、回复一次。

- [x] /help 控制面板：会话、模型、任务三组双列等宽按钮；停止独占底行，命令写在按钮上。
- [x] /resume 会话信息与恢复按钮使用垂直满宽布局；手机端实际排版验收通过。
- [x] 手机端卡片：带说明的会话和模型按钮使用垂直满宽布局；控制面板使用短标签。

- [x] /models 可用模型选择按钮；模型设置按用户/目录持久化。
- [x] 按用户和目录隔离会话文件；旧版单 ID 文件会在用户首次保存会话时兼容迁移。
- [x] 长回复按代码块边界拆分；进度卡和最终结果卡分离。
- [x] 加强按钮竞态与失效处理：审批归属校验；任务卡使用一次性标识，旧停止按钮不会影响新任务；重复停止请求幂等。
  服务重启后旧任务卡没有对应的活动标识，会安全拒绝。
- [x] /help 模型/恢复/压缩按钮飞书端联调通过；卡片点击和命令来源已记录为不含参数的结构化日志。
  已验证状态、模型列表、模型选择、恢复会话、压缩上下文。运行中会话切换会被串行化处理。
- [x] /status 显示当前用户任务状态、等待队列、会话、模型和本桥接运行时长，便于在飞书端诊断后台状态。
- [x] 飞书内工作目录切换：`/cd` 支持受限工作区内的相对/绝对路径、子目录快捷卡和创建确认；目录按用户保存，任务在入队时固定目录快照，会话与模型按用户/目录隔离。

## 已知环境约束

- 当前 Termux 项目目录 /home/angus/dev/feishu-codex-bridge，工作目录 /home/angus/dev。
- .env 包含凭据，不提交；不要在交接文档、日志或工具输出里泄露。
- 飞书卡片回传交互在“回调配置”，使用长连接。
- 当前项目有 lark-oapi CARD 帧兼容补丁，升级 SDK 时核对是否仍需要。
- 目前后台由工具终端运行；不能把终端会话编号当作可靠的服务管理。

- [x] 飞书专用代理：显式配置 REST、地址获取与 WebSocket 代理，修复 Fake-IP 无法直连时持续等待连接；增加离线回归。

- [x] 飞书默认跟随启动环境的代理变量和 NO_PROXY；移除本机固定代理覆盖，保留可选专用配置，新增环境代理/直连回归。

## Rust 重构：开始实施

约束已确认：Linux/Termux、最终全 Rust、允许 Python SDK 过渡、首版串行、JSON 状态、新部署显式授权。完整方案与阶段验收见 docs/rust-refactor.md。

- [x] 正式安装 Rust 1.85.1，固定 workspace 工具链与 Cargo.lock。
- [x] 六个生产 crate 与 xtask 边界检查；core 无网络/SDK 依赖。
- [x] 命令、任务/交互状态机、有界 FIFO、保存失败拒绝入队。
- [x] JSON 单写入者、fsync 原子保存、跨重启去重与损坏版本拒绝。
- [x] 旧状态 dry-run、导入、Python 格式导出；不读取真实 .env。
- [x] 路径解析、逐级 no-follow 普通文件打开、有界快照与文本 diff/成果分类。
- [x] CLI 配置检查、基础 CI、Rust 与 Python 跨语言回归。
- [ ] P0 性能测量、完整行为/协议 fixture 和既有待完成手机验收。
- [ ] P1 完整异步 ports、全部 Python 行为在 Rust 的对等回归。
- [x] P2 子集：有界双向 RPC、异步 backend、子进程握手/回收、0.153.4 schema 和 turn 契约。
- [x] P3 子集：REST/卡片/流式附件边界、SDK 薄进程、严格 IPC 解码和卡片命令往返回归。
- [ ] P2 完整 Codex app-server 执行器、连接代次、schema 与事件契约验证。
- [ ] P3 Rust 业务主循环、REST/卡片/附件与 Python SDK 薄进程。
- [ ] P4 Rust 飞书长连接、代理矩阵、Termux 实机与手机验收。
- [ ] P5 Rust supervisor、日志/健康、打包发布、持续运行和生产回退演练。

没有切换、重启或更改现有生产服务配置。Rust 前台最小版仍不能完整替代 bridge.py。

2026-09-07：39 项 Rust 与 54 项 Python 回归通过；Android 检查受 ring 所需 C/汇编编译器的 proot 包装故障阻塞，尚未通过当前完整 workspace 的 Android 验证。SDK ACK 持久化确认和主循环仍待实现。

- [x] 已从原始 Codex 会话逐字恢复完整重构计划，作为设计基线：`docs/plans/rust-refactor-original.md`；进度与基线分开维护，后续偏离通过 ADR 记录。
- [x] 飞书 REST 新增无网络响应测试：HTTP/业务错误、畸形/超大响应、路径参数隔离；Rust 回归累计 42 项通过。完整请求和附件契约仍待补齐。

后续重构同时对照 `docs/plans/rust-refactor-original.md` 与本文件；原始方案保留不变，偏离记录 ADR。
- [x] P2 事件子集：输出/完成/归档映射到应用类型，保留执行身份，拒绝缺失身份和未知终态。应用事件消费、审批/问答/Plan 仍待完成。
- [x] ADR 0001 记录显式 boxed future 与原方案 async-trait 的实现差异。

本轮验证：44 项 Rust 测试通过；fmt、Clippy（-D warnings）、依赖边界和 diff 空白检查通过。未进行真实飞书/Codex 验收。

- [x] P2：命令/文件审批与逐题问答映射、一次性回复句柄、未知请求错误响应；增加版本 schema 与 Rust/Python 共用 fixture。
- [x] P2/P3：交互归属、容量、期限与部分答案注册表；完成先于启动响应的有界缓冲；Plan 完成项和失败详情映射。
- [x] P3：SDK 等待 Rust 接受确认后应答，超时/队列满失败；Rust 有界 IPC pump、一次性确认句柄和首次连接生命周期通知。
- [ ] 将上述端口接入完整业务 actor、耐久认领与 supervisor；目前 ACK 接口测试通过并不代表生产持久化链路已完成。
- [ ] Codex additionalPermissions/networkApprovalContext 已完成一次性审批解析、展示、回传与回归用例，待统一验证；会话/永久授权及远端环境审批仍未开放。

- [x] P3 会话准备：新建/恢复成功后先耐久保存绑定，再启动 turn；恢复 ID 不匹配拒绝执行，保存失败不启动，执行失败保留绑定且不重试。
- [x] JSON 异步访问：最多两个阻塞任务、同一存储锁串行提交；调用者取消不打断已经开始的耐久写入。
- [x] 异步接收预留/提交：磁盘完成乱序仍保持 FIFO；容量包含待保存项，取消后迟到的保存结果不复活任务。
- [ ] 完整运行 actor 仍待装配；新增跨 crate 测试连接 IPC、真实 JSON 去重和调度，不能替代生产接收与 supervisor 验收。

## 优先交付：前台最小运行版

- [x] `bridge run --config`：Rust 业务、Codex 适配、飞书 REST 与 Python SDK 薄进程装配。
- [x] 白名单文本任务、串行队列、耐久认领、会话连续、纯文本结果、/help、/status、/stop。
- [x] 明确拒绝尚未开放的人工审批，问题返回空答案；不自动批准、不自动重放任务。
- [x] 复用 Python App ID 全局锁、处理 Ctrl-C/TERM、关闭 SDK 和 Codex 进程组。
- [x] 最小 actor 离线集成测试与使用文档 `docs/minimal-runtime.md`。
- [x] 用户授权复用停用的原机器人完成最小版单用户真实运行验收。
- [ ] 原生 Android/Termux 构建与运行验证仍待完成。
- [ ] 后续恢复完整命令、卡片/审批/问答界面、附件/Plan 和完整 supervisor；不扩大本次最小版范围。

检查点（按用户额度安排暂停）：70 项 Rust 测试、58 项 Python 测试通过；fmt、Clippy（-D warnings）、依赖边界、rustdoc（-D warnings）与 diff 空白检查通过。最小版 CLI 已构建并验证帮助入口，尚未进行真实飞书/Codex 联调或切换生产服务。本批改动保留在 development 工作区，尚未提交或推送；下一步优先按 `docs/minimal-runtime.md` 完成独立机器人验收。

- [x] 停止测试收尾：等待 fake Codex 收到第二个 `turn/start` 并发出响应后再测试停止；同一聊天中另一位已授权用户停止无效，任务仍运行；所属用户停止后必须收到中断完成，RPC 记录严格为一次指定 thread/turn 的 `turn/interrupt`，不再接受“准备阶段已停止”作为成功。相关最小运行版集成测试 2 项通过；本次仅加强测试，未修改运行实现。
- [x] 真实连接检查点：用户确认旧服务停用并授权复用现有配置；确认旧服务锁空闲后启动 Rust 最小版，Codex 初始化成功、SDK 显式报告 Connected。本地私有配置/启动辅助文件及日志均被忽略，Rust 独立状态目录未覆盖旧会话文件。此检查只证明真实连接成功，飞书端命令回复、模型回答、停止和重启仍待人工验收。
- [x] 基础收发真实验收：用户确认 `/help`、`/status` 和“只回复 hello，不修改文件”均正常收到回复；本地 Codex 会话记录也确认任务完成、最终回答为 `hello`。运行日志未发现发送失败；观察到飞书重连后恢复 Connected。串行排队、会话复用、运行中停止及跨用户隔离、退出和重启仍待真实验收，不能据此认定完整迁移完成。
- [x] 会话复用、排队和停止真实验收：用户追问上一轮回答得到 `hello`；运行任务期间第二项显示等待 1，`/stop` 取消 1 项等待请求并返回任务已停止。Codex 记录共 3 次启动、2 次完成、1 次 turn_aborted，无 queued 回答；停止结果附带的是已有过程文本。
- [x] 空闲退出与重启检查：TERM 后此前追踪的 5 个所属进程均不再运行；重新启动后飞书恢复 Connected，保留 1 个会话绑定，观察时任务启动次数仍为 3，未自动重放已取消任务。
- [x] 重启后会话恢复：用户确认重启后的首条追问正确回复最初测试的英文单词 `hello`。
- [x] 运行中关闭检查：确认桥接所属进程中存在正在运行的 sleep 工具后发送 TERM，追踪的 6 个进程（含 Rust、SDK、Codex 和工具）均不再运行；重新启动后飞书恢复 Connected，观察时任务启动数仍为 7，未生成 finished 回答，未发现自动重跑。此次关闭未在 Codex 会话文件新增 turn_aborted，清理结论依据进程身份和存活检查，不将其记为已收到协议中断完成。
- [x] 自然排队的本地执行记录包含依次完成的 first、second 回答；用户确认飞书端顺序正常。
- [x] 用户确认运行中关闭并重启后 `/status` 空闲、新任务正常，最小版单用户真实验收通过。
- [ ] 跨用户停止隔离因只有一个测试用户，保留离线通过、真实验收待补。

## P3 后续：命令逐项迁移

- [x] `/new` 应用层接入：与 Python 行为一致，全局存在运行、排队、待保存或会话变更时拒绝；空闲时仅耐久清除当前用户/目录绑定，下次提问才创建 Codex 会话。
- [x] `/new` 复用持久化认领，近期旧消息重投不清除新绑定；先认领后写入失败需新消息重试，不自动回放。去重保留 1,000 条的现有限制不变。
- [x] `/new` 离线回归：新增 5 项测试并加强运行中拒绝检查；全工作区 75 项 Rust 测试、fmt、Clippy（-D warnings）、依赖边界和 diff 空白检查通过。
- [x] `/new` 真实飞书验收：新版本提交 `fe6be3e` 构建并重启后，用户确认 `/help` 已显示 `/new`，发送 `/new` 成功返回新会话提示；线上进程运行新版本并已连接飞书。
- [ ] 后续按独立改动迁移 `/resume`、`/compact`、模型/目录/Plan 命令，再补交互界面、附件和服务监督；每项继续对照原始方案验收，不将 P3 标记完成。

- [x] `/resume` 文本入口：当前目录最近 8 项列表、全局空闲门控、read/resume 双重 ID/目录/活动状态校验、持久化后回复成功。会话可见范围保持 Python 的目录边界。
- [x] `/resume <ID>` 先持久化认领并确认 SDK，再异步恢复，避免 RPC 延迟阻塞回调；重复消息不替换新绑定，失败不自动重试。
- [x] `/resume` 离线检查：新增 6 项用例并扩展 actor 集成测试，全工作区 81 项 Rust 测试、fmt、Clippy（-D warnings）、依赖边界和构建通过。
- [x] `/resume` 已部署：确认空闲后 TERM 关闭旧进程及后代，重启并核对新进程二进制身份、本次启动后的 Connected 事件；本批源码尚未提交。
- [x] 用户确认包含 `/resume`、模型和 Plan 命令的版本真实验收通过；卡片列表与分页仍未迁移。

- [x] 模型和 Plan 命令批量接入：`/models`、`/model [ID|default]`、`/plan [on|off]`；按用户/目录持久化，校验模型后保存，变更使用耐久认领和全局空闲门控（ADR 0002）。
- [x] 任务准备加载设置并传入 turn/start；Plan 完成项优先于过程输出作为最终文本，保留 Unicode 安全的 32 KiB 截断。`/new` 和恢复会话保留设置。
- [x] 本批全量检查：83 项 Rust 测试、fmt、Clippy（-D warnings）、依赖边界与构建通过；测试覆盖实际 turn/start 的模型/Plan 参数、正式计划正文优先、无效模型、忙时拒绝、重投、持久化与写入失败。
- [x] 本批已部署：空闲时关闭旧进程及后代，新进程二进制身份与当前构建一致，并观察到本次启动后的 Connected 事件。源码连同前批 `/resume` 改动尚未提交。
- [x] 用户确认本批模型/Plan 真实验收通过，按要求提交并推送；Plan 实施卡片和问答仍待后续迁移。

- [x] 会话归档命令组：`/archive <ID>`、`/archived`、`/unarchive <ID>`，复用恢复命令的全局空闲门控和持久化认领；列表按当前目录过滤。
- [x] 远端归档成功后一次提交清除所有指向该 thread 的本地绑定，保留其他会话与偏好；取消归档不切换当前会话。归档结果不确定或本地清理失败时报告并停止，待人工核对修复，不自动重试。
- [x] 归档命令组 Rust 离线检查：91 项测试通过，fmt、Clippy（-D warnings）、依赖边界、构建、rustdoc（-D warnings）通过。新增远端返回错误会话 ID、缺失目录时不得归档的回归，并验证跨目录的同 thread 绑定一并清理。
- [x] Python 回归本轮运行 58 项：56 项通过、2 项 schema 校验因缺少依赖跳过；不将跳过项记为通过。
- [ ] 归档命令组部署与真实验收；外部客户端归档事件同步和自动部分失败协调仍待实现。
- [x] `/compact` 文本入口：先持久化认领，再读取并加载当前绑定会话；read/resume 双重校验 ID、目录和空闲状态，不创建新会话、不修改绑定或偏好。
- [x] 异步压缩生命周期：映射 `turn/started` 并绑定执行身份；请求响应和完成通知均到达后才释放全局互斥，处理完成先于响应、失败和中断。`/status` 显示压缩中，所属用户可 `/stop`；准备阶段停止不发送压缩请求。压缩期间拒绝新任务与会话变更，结果不确定时停止运行且不重试。
- [x] 本批离线回归：101 项 Rust、58 项 Python 测试通过。补齐 Python schema 校验依赖，此次没有跳过项；新增压缩互斥、跨用户停止、持久化去重、保存失败、已有会话加载、身份/目录拒绝、提前完成与畸形响应回归。
- [ ] 用户要求优先继续功能开发，归档与压缩部署/真实飞书验收统一延后；本轮未切换或重启服务。后续继续迁移目录命令、卡片交互与附件，P3 仍未完成。

- [x] `/cd` 已有目录浏览和切换：支持工作区内相对/绝对路径、空格和上级目录，最多展示 20 项子目录；缺失目录、文件、越界和失效目录快照被拒绝，不自动创建或静默回退。
- [x] 目录按用户持久化并在重启时读取；切换使用全局空闲门控和耐久认领，保存成功后才更新运行时选择。任务固定目录快照，turn 显式指定 cwd 和同目录可写范围；会话、模型、Plan、归档和压缩使用用户当前目录。阶段性门控差异记录于 ADR 0003。
- [x] 本批 106 项 Rust、58 项 Python 回归通过；新增目录保存/失败、越界/符号链接、运行时缓存、执行目录/设置隔离、忙时拒绝和跨重启重投测试，更新 turn 协议样例及 schema 校验。
- [x] 缺失目录文本创建确认：`/cd` 先给出目标与 `/cd-confirm <编号>`，绑定用户/聊天/原目录，10 分钟有效、最多 100 项；忙时拒绝不消费，成功切换、新提示、过期和重启使旧确认失效。
- [x] 确认先耐久认领，执行前重新解析原始输入并核对目标；目录描述符逐级创建且不跟随符号链接，创建后保存成功才切换。创建/保存失败保留已建目录，不自动删除或重试。
- [x] 本批 109 项 Rust、58 项 Python 测试通过；fmt、Clippy（-D warnings）、依赖边界、rustdoc（-D warnings）、构建与空白检查通过。覆盖确认归属/过期、忙时重试、一次性消费、重启失效、路径替换和创建后保存失败。
- [x] 接通 `/help` 控制面板与目录创建确认卡片，发送失败回退文字命令。回调经 SDK IPC 和 gateway 解码，只接受本地登记的操作编号；绑定实际卡片 message ID、用户、聊天、目录和 10 分钟有效期，每个按钮一次使用。
- [x] 按钮复用原有命令处理与持久化认领，以按钮编号区分同卡片的多个动作；错误来源不消费操作，目录变更使旧卡片失效。停止按钮额外绑定任务/队列快照，防止旧卡片影响新任务。
- [x] 本批最终 111 项 Rust、58 项 Python 回归通过；fmt、Clippy（-D warnings）、依赖边界、rustdoc（-D warnings）、构建和空白检查通过。新增卡片渲染到 gateway/运行时的闭环、同卡片多动作、错误来源/用户、一次性消费、目录失效和旧停止按钮回归。全量测试曾出现一次卡片用例超时，随后定向及全量复跑均通过，原因未确定，未放宽时限。
- [ ] 下一项：迁移会话与模型列表卡片，并补原卡片刷新/失效状态更新；随后继续审批、问答和附件。卡片操作本轮未部署，真实飞书点击和手机布局验收仍延后。

- [x] Rust 会话/模型列表卡片：最近与归档列表提供恢复、归档、取消归档和导航按钮，模型列表提供选择及恢复默认；复用一次性回调校验。停止快照校验仅适用于停止按钮。
- [x] 列表体验补齐：会话标题、完整 ID、活动状态和可复制命令保留在正文，按钮用短编号并采用满宽布局；模型按钮取消横向挤排。刷新入口发送新列表，空列表仍可导航；文字降级保留会话与命令对应关系。
- [ ] 下一步：原卡片消费/失效状态更新及刷新；随后接入审批、问答和附件。真实飞书验收继续延后。

本轮列表体验检查：bridge-app 与 bridge-feishu 共 27 项测试通过，应用层 Clippy、fmt、依赖边界及空白检查通过。未部署或进行真实飞书验收。

- [x] Rust 原卡片失效更新：记录成功发送的卡片视图，根据一次性登记表移除已消费、到期、目录失效和旧停止按钮；保留其他可用操作，全部失效后提示重新打开。
- [x] 更新请求串行且有 45 秒超时，避免并发提交不同版本；失败记录日志，不重放业务、不恢复按钮权限、不自动重试。视图最多 1,000 张，全失效更新提交后释放。
- [ ] 下一步：列表原位重新查询/刷新；之后继续审批、问答与附件。重启前卡片主动失效更新及真实飞书验收仍待完成。

本轮验证：Rust workspace 全量通过，新增消费/过期与目录/停止失效测试，卡片运行时回归模拟更新失败后业务仍仅执行一次。Clippy（-D warnings）、fmt、边界及空白检查通过。Python 58 项中 56 通过、2 项 schema 校验因缺少依赖跳过；本轮未部署。

- [x] Rust 会话与模型列表原位刷新：从列表卡片点击刷新或最近/归档导航，先撤销该消息所有旧按钮，查询后沿用原 message ID 更新。文字命令及控制面板入口仍发送新卡片。
- [x] 刷新与失效更新共用串行通道，待刷新队列最多 128 项；更新成功才登记新按钮，登记时复核目录代次。更新失败降级文字命令，不恢复旧按钮，不自动重试查询或操作；查询失败报告错误，需重新发送列表命令。
- [ ] 下一步：接入人工审批卡片及同意/拒绝回传；随后迁移问答与附件。真实飞书验收继续延后，本轮未部署。

本轮验证：模型原位刷新、旧/新按钮权限、更新失败文字降级、最近到归档原位导航集成测试通过；Rust 串行全量回归通过，Clippy、fmt、依赖边界和空白检查通过。默认并发全量中两项既有压缩测试曾报 Locked，8 项压缩定向串行及全量串行复跑通过；并发锁失败根因尚未确认，不记为已修复。
