# Rust 项目结构与工程质量整改计划

## 目标与执行原则

本计划完整替换旧计划，不继承旧计划的“已完成”结论。事项来自代码、Cargo 元数据、脚本和测试检查；README、AGENTS.md 与其他说明都是需要随实现校正的材料，不是阻止本计划整改的架构约束。

目标是形成职责清晰、可独立部署、构建产物可靠、测试覆盖真实执行路径的 Rust 项目。保持现有用户功能与持久状态兼容。下文三个阶段是问题分类，不是要求一个 agent 顺序执行；分散在不同设备的独立 agent 按“多设备独立开发与合并约定”领取工作包。

### 交接状态（2026-09-14）

- 本次文档整理：覆盖旧计划；删除旧设计、迁移说明和过时 ADR；修正保留文档的现状描述及发布脚本的文档来源。
- 二进制部署说明归并到部署指南，供源码仓库和发布包使用。
- 下列工程整改尚未执行，除明确勾选的文档子项外均为待办。不得把计划中的新命令、模块或接口当成已经存在。（该表述已被下文 2026-09-18 状态更新取代：第一至第三阶段 A–F 工作包均已完成。）
- 上一轮审查的基线验证通过：workspace 测试、fmt、Clippy、产品 debug 构建、rustdoc、依赖边界、Shell 语法和 diff 检查。这不证明本计划列出的问题已经修复，也不代替修改后的验证。
- 真实飞书交互、目标主机生命周期和长时间稳定性尚未在本次审查中验证。

默认决策：

- 继续支持 Linux，暂不扩展 Windows/macOS。
- 保留现有六个生产 crate；优先调整模块职责，不为拆分而增加生产 crate。
- Shell 只承担工具链引导和入口转发；配置、业务校验、构建产物处理尽量由 Rust 负责。
- 保持单任务执行、审批归属校验、耐久认领、禁止自动重放结果未知操作等行为。
- 不自动部署，不删除用户配置、状态、日志或附件。不得借清理旧文档重置现有 Rust 状态。
- 已有测试通过只是基线，不代表下列问题已经获得验证。

### 状态更新（2026-09-18）

- 工作包 F（`refactor/ports-diagnostics`）已在汇总分支完成：任务文件端口与消息传输端口拆分、
  文件策略上移 `bridge-app`、飞书事件转换收回 `bridge-feishu`、诊断改为可注入句柄并补齐
  交付/卡片/健康/过载事件、启动失败脱敏记录、`TaskId`/`CardToken` 专用类型、存储与 runtime
  错误分类化。第 10 项补充慢传输与命令洪峰测试。
- 工具链改为跟踪 Rust stable（`rust-toolchain.toml`），不再固定 1.85.1。
- 上述条目勾选以本分支 `cargo xtask check` 全绿为验证结果；合入主分支与部署仍由维护者决定。

## 架构可述性重构（2026-09-19 起，工作包 G–M）

全仓扫描确认约 30 处"实现过于复杂、难以用文档描述、人类难以阅读"的热点，集中为六个工作包。目标：让每个子系统等于一个代码模块加一个文档小节加图上一个节点；每条跨子系统交互是有名字的边（channel/port），文档中的图与代码符号一一对应。默认决策沿用上文：不改 crate 划分，在 crate 内调整模块职责；保持架构说明中"Runtime invariants to preserve"的全部不变量、用户可见文案与持久化格式不变。以下条目在对应提交合入后勾选。

### G `refactor/runtime-bus` — 运行时总线与组件化

- [x] `Done` 按因果流分域为 Session/Task/Card/Delivery 子枚举；`JoinSet<Done>` 单通道结构不变；`handle_done`（约 995 行、26 分支）改为分派加按域处理，每个函数不超过 100 行。
- [x] `Runtime` 的 29 个字段收拢为 Admission、TaskTrack、CardBook、SessionBook、Presentation 五个命名组件；组件方法只修改自身字段，跨组件读取通过显式参数。
- [x] `Input.accept` 闭包改为具名 `Ack` 句柄（Drop 即 NAK，与现状每路径恰好结算一次等价），`bridge-feishu` ingress 同步改造。
- [x] 运行时数据流图（6 个异步任务与 4 条有界通道及容量）进入 `docs/architecture.md`；ASCII 拓扑图进入 `runtime/mod.rs` 模块文档；`Ack` 契约成文。
- [x] 魔法数 `64`（`jobs.rs`）改用 `limits::EARLY_PROTOCOL_EVENTS`。

### H `refactor/outcome-model` — 结果模型

- [x] 引入 `Outcome` 枚举，在 `flow::finish` 与协议事件边界携带；`presentation.rs` 不再用中文消息前缀做控制流，文案只在展示层生成且逐字不变（卡片标题判类型归入工作包 I 的 `CardBook`）。
- [x] 结果分类与 tone/文案映射表进入 `docs/runtime-flows.md`。

### I `refactor/card-grants` — 卡片授权模型

- [x] 新增 `runtime/tokens.rs`：`panel-`/`cd-`/`approval-`/`plan-`/`card:` 五种令牌格式的唯一铸造点。
- [x] `CardBook` 成为卡片校验唯一入口，文档写明统一失效规则（签发绑定 source+generation+任务快照、一次性消费、deadline、停止快照比对）。
- [x] 无名元组 `stop_snapshot: (u64, Option<TaskId>)` 改为具名 `TaskSnapshot`。
- [x] `deliver_list` 参数收敛，删除调用方 `next_panel + 1` 预言。
- [x] 卡片令牌生命周期时序图进入文档。

### J `refactor/supervision-plane` — 监督与控制平面

- [x] `supervisor_state::Phase` 成为唯一权威状态机；控制 socket 从自由字符串（含 `backoff:N`）改为行 JSON 编码的枚举；删除 `health.rs` 与 `service_control.rs` 的猜测式解析。
- [x] `run_layer`/`supervise` 的 `action: &str` 与 `outer: bool` 改为 `Layer` 枚举；120s/45s/20s/30s 阈值常量化并互注释。
- [x] `bootstrap.rs::run`（约 326 行）提取归档对账与 gateway 路由循环为具名函数，合并重复的成功聚合。
- [x] guard→supervise→run 进程链图与监督状态机图进入文档。

### K `refactor/transport-readability` — 传输子系统可述性

- [x] `bridge-feishu`：`session()` select 分支拆具名处理器并常量化心跳/背压/回执阈值；重连返回值改 `AttemptOutcome` 并命名 60 秒稳定连接阈值；`Fragments::push` 的 signature 五元组具名化并逐条注释上限；`rest.rs` 提取 `authenticated_post` 统一认证与状态码检查；`attachments()` 拆分并命名哨兵值；`proxy.rs` 补协议阶段注释。
- [x] `bridge-codex`：`events.rs::notification()` 按 method 拆函数并常量化截断阈值；`requests.rs::decode()` 的 5 个审批否决条件具名化；`transport.rs` 封装有界等待样板；`permissions.rs` 校验与渲染分离；`backend.rs` sourceKinds 提常量注明来源。
- [x] 飞书会话/重连状态机图与 Codex 协议映射表进入文档。

### L `refactor/local-safeio` — 安全边界与测试契约

- [x] 新增 `bridge-local/src/safeio.rs`：统一 NOFOLLOW 逐组件走路径（现三份重复）、原子发布（现两份重复）与 `pinned_path`；`bridge-cli` 的五处安全打开改用。
- [x] `async_state.rs` 校验常量化并提取助手，`key()` 持久化格式成文；`snapshot.rs::scan_excluding` 拆分职责并命名 20MB 上限。
- [x] `fake_codex_runtime.rs` 顶部补 mode→行为→信号文件契约表；`runtime_flows.rs` 按 mode 拆分断言函数。
- [x] 文件访问安全边界一节（威胁模型与原语清单）与测试架构节进入文档。

### M `docs/architecture-home` — 文档总装与词汇表

- [x] `docs/architecture.md` 重组为架构主页：系统上下文图、进程链图、运行时数据流图、五组件节、不变量节与词汇表（概念、代码符号、图节点三列对应）。
- [x] `docs/runtime-flows.md` 时序图集（文本任务执行、/cd 切目录、compact、卡片点击校验）。
- [x] `docs/operations.md` 补监督状态机与健康 Phase 含义；`docs/development.md` 补测试架构与 fake 契约表；`docs/deployment.md` 补 `bridge credentials`、`check-command`、`--danger-full-access`。
- [x] README 校验清单补 Codex 协议快照校验与单独命令入口。
- [x] 文档-代码对照走查：每张图的节点与边能在代码中找到同名模块、通道或类型。


## 第一阶段：修复确定的问题

### 1. 修复独立 status 命令的计时器缺失

问题：`bridge-cli/src/main.rs` 的 `Action::Status` 使用仅调用 `enable_io()` 的 Tokio runtime，随后调用包含 `tokio::time::timeout` 的服务控制接口。服务存活并进入该分支时存在 panic 路径。

- [x] 统一 `status` 与 `service status` 的查询实现，启用所需的 IO 和计时器。
- [x] 清理 CLI 中重复的 runtime 创建与状态展示逻辑。
- [x] 用临时锁和本地控制 socket 验证服务存在、控制接口超时、服务不存在三种情况；通过真实 CLI 子进程覆盖该分支，不启动长期服务。

### 2. 修复发布包无法独立部署的问题

审查时的问题：`package.sh` 打包的 `DEPLOYMENT.md` 是跳转页，引用未随包提供的文档和脚本。本次文档整理已更换来源并补充二进制使用步骤；仍需完成产品输出和自动化验证。

- [x] 在部署指南中完整描述二进制包的配置生成、凭据提供、启动、状态、停止和校验；发布脚本打包这份指南。
- [x] 二进制部署章节不依赖源码目录，源码脚本步骤明确标为另一种安装方式。
- [x] 修正 CLI 输出中固定提示 `./start.sh status` 的内容，使二进制直接使用也成立。
- [x] 增加包文件清单与文档相对链接检查。

### 3. 保证打包的是本次构建的产物

问题：构建完成后固定读取 `target/release/bridge`，无法正确处理 `CARGO_TARGET_DIR` 或 target 配置，旧文件存在时可能误打包。

- [x] 实现 `cargo xtask package [输出目录]`，保留 `package.sh` 作为转发入口。
- [x] 从 Cargo JSON 构建消息取得本次 `bridge` executable 路径；禁止回退猜测旧路径。
- [x] 当前发布仅允许主机目标；检测到非主机 target 时明确报错。
- [x] 由 Rust 完成复制、SHA256 清单和构建信息生成。
- [x] 构建信息记录 commit、dirty 状态、实际 target、工具链和包版本；包含未跟踪源码导致的 dirty 状态，默认拒绝脏树发布，提供显式 `--allow-dirty`。
- [x] 先在临时目录完成组装与校验，再发布最终目录，避免半成品看起来像成功产物。
- [x] 覆盖自定义 target 目录、含空格路径、旧产物存在、构建失败及重复打包。

### 4. 清理迁移残留和错误描述

- [x] 删除 Dependabot 中的 pip 更新任务。
- [x] 删除描述旧变量、`Bridge.command()` 和过时交付状态的设计与迁移文档。
- [x] 删除依赖旧阶段编号、旧计划和失效待办的 ADR；仍有效的运行约束归并到架构说明，不保留旧计划的隐藏要求。
- [x] 清理代码中的 MVP、minimal、过渡期注释。
- [x] 保留文档明确区分 Rust 服务、Shell 工具、C 编译工具链、Linux 系统接口和外部 Codex 依赖。
- [x] 完成后再次检查所有文档与实际命令、代码及测试一致。

## 第二阶段：整理真实运行路径与职责

### 5. 消除并行存在的交互状态实现

问题：`bridge-app::interactions::Registry` 及相关 core 类型有自己的测试，但实际 runtime 使用 `PendingApproval` 等另一套流程，容易出现测试覆盖与生产实现脱节。

- [x] 以当前 runtime 的行为为迁移基线，把审批、逐题问答、过期、取消和一次性回复抽成唯一交互管理模块。
- [x] 替换或删除旧 Registry 和只服务于它的类型，不保留两套完整状态机。
- [x] 将旧测试中仍有价值的约束迁移到生产实际调用的实现。
- [x] 必须覆盖归属校验、旧 epoch、早到请求、重复点击、逐题答案、过期、不完整答案禁止提交，以及回复结果未知后的处理。

### 6. 拆分巨型 runtime

问题：审查时 `runtime.rs` 约 1,515 行，`run()` 集中了大量状态、命令和异步完成分支。

- [x] 引入明确的运行状态结构，分别管理执行、交互、卡片和后台任务状态。
- [x] 提取输入处理、协议事件处理、后台完成处理、定时器处理和关闭流程。
- [x] `tokio::select!` 只负责选择事件和调用处理方法，避免把整个业务流程塞入宏体。
- [x] 用枚举表达关键互斥阶段，减少多个布尔值和 `Option` 组合产生非法状态的空间。
- [x] 保持现有串行调度和事件顺序；此次不放宽目录、会话、模型变更门控。

### 7. 分离文件访问、交付策略与消息传输

问题：`bridge-local::Delivery` 同时负责文件扫描、附件限额、卡片生成、文本回退和上传编排；`Messenger` 混合消息发送与任务文件生命周期。

- [x] `bridge-local` 保留安全文件打开、临时文件发布、快照、差异和稳定文件句柄。
- [x] `bridge-app` 负责成果选择、数量限制、展示、上传顺序和失败反馈。
- [x] 拆分消息传输端口与任务文件端口，移除 `Messenger` 中混杂的文件准备、绑定和收尾职责。
- [x] 继续通过已验证文件句柄交付，不退回任意路径上传。
- [x] 将飞书原始消息与卡片 JSON 到应用输入的转换收回 `bridge-feishu`；CLI 仅做组装和转发。

### 8. 收敛类型与错误边界

问题：部分边界已类型化，但运行状态仍大量使用字符串；配置错误、状态存储错误和运行错误的分类不一致。

- [x] 对容易混用的任务 ID、卡片 token 和服务控制动作采用专用类型；无需机械包装全部字符串。
- [x] 本地控制接口以枚举封装动作与响应，集中维护现有字节编码。
- [x] 为配置、存储和 runtime 提供可分类错误，保留内部原因与安全的用户提示映射。
- [x] 配置层保留 TOML 表示，集中转换到应用 sandbox 类型，消除散落映射。
- [x] 保持持久化格式不变；内部类型重构不得隐式重置已有状态。
- [x] 继续禁止把原始远端错误、凭据和用户正文直接放入日志。

### 9. 统一诊断出口并保留启动失败信息

问题：现有诊断 Sink 与直接 `eprintln!` 的 JSON 事件并存，后台服务丢弃 stderr 后会丢失部分事件；全局 OnceLock 也限制重复组装。

- [x] 所有服务运行事件进入类型化诊断接口，补齐交付失败、卡片失败、健康写入失败和过载事件。
- [x] runtime 接收可注入诊断接口，默认提供空实现供测试使用。
- [x] 持久输出、时间、PID、序列化和日志轮转放在基础设施实现中。
- [x] 为诊断初始化前的启动错误提供受限、脱敏的记录或回传路径，禁止捕获并原样保存任意 stderr。
- [x] 测试后台失败可观测、日志轮转、日志写入失败，以及多个测试实例互不污染。

### 10. 调整资源上限的执行方式

问题：已有有界通道，但后台任务在 `jobs.len() > 128` 后通过停止整个 runtime 处理过载；多处限额和超时散落。

- [x] 集中定义现有运行限额和超时，首先保留原数值，不扩大 TOML 配置面。
- [x] 后台任务在创建前检查容量，并为停止、审批回复、关闭等控制操作保留独立容量。
- [x] 普通用户请求过载时返回繁忙结果，不仅因为后台数量触顶就终止所有任务。
- [x] 协议结果未知、耐久状态不可信等必须停止的情况继续停止。
- [x] 验证慢传输、命令洪峰、审批风暴和关闭并发下的有界性与控制响应。

## 第三阶段：完善开发、安装和验证工具

### 11. 隔离测试工具和重复测试装配

问题：fake Codex 是生产 package 中的普通 `[[bin]]`；CLI 集成测试重复构造消息、状态与运行环境。

- [x] 设置 `bridge-cli` 的 `default-run = "bridge"`。
- [x] 建立非发布的测试支持包，集中假进程和公共测试装配；生产依赖不得引用它。
- [x] 测试入口先构建假进程，再把绝对路径显式传给需要的测试；禁止测试中递归构建 Cargo。
- [x] 从生产 package 移除 fake binary 目标。
- [x] 应用行为测试尽量使用端口 fake；真实 JSON-RPC 假进程测试保留在协议层及少量跨层集成场景。
- [x] 开发工具与测试假程序不出现在发布包和产品文档中。

### 12. 收拢安装与配置规则

问题：Shell 和 Rust 重复校验配对码，Shell 固定使用默认凭据变量，而 TOML 支持自定义变量名；setup 检查 PATH 中的 `codex`，不能准确代表配置中的 executable。

- [x] 把配置感知的凭据读取、隐藏输入和校验集中到 Rust CLI。
- [x] Shell 保留 Rust/C 工具链检查和用户明确选择后的安装引导。
- [x] 已有配置按其声明的环境变量和 executable 工作，不额外强制默认变量名或 PATH 中的 `codex`。
- [x] 新配置继续使用当前默认环境变量，已有配置不覆盖。
- [x] `config check` 完整验证静态配置，包括环境变量名、路径和必填字段，但不要求网络、登录态或实际凭据。
- [x] 统一配对码的字节长度和 Unicode 空白判断。
- [x] 二进制部署路径不要求 Rust、Cargo、源码 checkout 或安装脚本。
- [x] 覆盖自定义凭据变量、已配对用户、非交互运行、Unicode 空白和缺失工具的情况。

### 13. 改善 workspace 与依赖门禁

- [x] 明确 workspace 成员和默认成员，开发工具不默认充当产品入口。
- [x] workspace 集中版本，各 crate 按需开启 Tokio、tokio-util、rustix 等 features。
- [x] 保持 Rust stable 基线（由 `rust-toolchain.toml` 指定）；本次不进行无关依赖升级。
- [x] 边界检查明确登记每个生产 crate 和开发工具，新 package 未分类时失败。
- [x] 分别检查 normal、build、dev 依赖，允许测试需要的方向，但禁止生产链路依赖测试工具。
- [x] 对禁止边、合法测试依赖、重命名依赖和新增 package 添加门禁测试。

### 14. 让协议测试验证实际编解码

问题：现有 schema 测试主要校验静态 fixture 与摘要，不能独自证明生产序列化输出符合 schema。

- [x] 保留版本化 schema、来源命令和文件摘要。
- [x] 捕获实际 backend 请求与审批回复序列化结果，用固定 schema 校验。
- [x] 使用请求 fixture 驱动实际解码，断言类型化应用事件和必要字段拒绝行为。
- [x] 将协议基线版本集中管理，消除测试中的重复路径常量。
- [x] 普通测试完全离线；schema 更新必须使用显式维护命令，不在 CI 自动访问真实 Codex。

### 15. 建立统一验证入口

- [x] 实现 `cargo xtask check`，统一 fmt、Clippy、workspace 测试、产品构建、rustdoc、依赖边界、仓库卫生、Shell 语法及打包结构检查。
- [x] CI 和开发文档只维护这一入口及其必要选项，避免命令逐渐漂移。
- [x] 依赖获取与离线测试执行分开；回归测试不访问真实飞书、Codex 登录态或用户状态。
- [x] 默认继续串行执行测试；测试隔离完成后再单独评估并行，不作为本次顺带变更。
- [x] 卫生检查覆盖 Git 跟踪的源码、清单、脚本和工作流；不能因为检查文件自身含禁用词就排除所有 YAML。
- [x] CI 增加 release 构建和包内二进制离线冒烟检查。
- [x] 文档生成使用 warnings-as-errors，测试失败保留可定位但脱敏的诊断。

## 多设备独立开发与合并约定

### 协作模型与共同基线

每个 agent 相当于一名独立开发者，由用户分别在不同设备启动，使用自己的 clone、分支、构建环境和测试数据。本计划不要求任何 agent 创建子 agent、集中调度其他 agent、共享会话、共享文件系统或持续在线。worktree 只是在同一设备处理多个任务时的可选工具，不是跨设备协作机制。

用户或仓库维护者先将本计划及文档清理提交并推送到远程，记录共同基线 commit（记为 B0），再创建远程汇总分支 `integration/rust-remediation`。B0 必须是所有设备能 fetch 到的具体 SHA，不能使用尚未提交的本地改动。此说明不是要求当前修改计划的 agent 自动推送、开 PR 或启动开发。

任务认领和交付以用户分配、远程 issue/PR 与 commit 为依据，不依赖 agent 私有上下文。用户或维护者负责批准归属变更、审查与合并；这是普通代码托管工作流中的角色，不是父 agent。各开发分支只推送自己的提交，默认不直接更新汇总分支或主分支。

### 工作包：第一批可同时开工

下面 A–D 都从 B0 创建分支，不等待其他工作包完成，不引用其他 agent 未合并的代码。表中 crate 路径均位于 `crates/`；“允许写入”范围外的文件只能读取，新增文件也必须落在本工作包的归属范围内。

| 工作包 / 分支 | 任务范围 | 允许写入 | 本批禁止事项 |
| --- | --- | --- | --- |
| A / `fix/cli-lifecycle` | 第 1、12 项；第 2 项 CLI 提示；第 8 项配置错误、本地控制类型和 sandbox 转换 | `bridge-cli/src/`；CLI 的 config、scripts 测试；`setup.sh`、`start.sh`；新增 CLI 测试使用 `cli_` 前缀 | 不改 Cargo 清单、行为集成测试、应用端口、全局日志接口及假进程 |
| B / `refactor/runtime-flow` | 第 5、6、10 项；第 8 项仅限内部运行状态；本范围过时注释 | `bridge-app/src/` 中除冻结接口外的实现；`bridge-core/src/interaction.rs` 及 `lib.rs` 中对应模块导出；CLI 的审批、卡片、目录、会话、文件及 minimal_runtime 行为测试；新增测试使用 `runtime_` 前缀 | 不改公共端口签名、其他 core 类型、CLI 生产代码、Cargo 清单、任何 fake_codex.rs 或通用测试装配 |
| C / `test/protocol-contracts` | 第 14 项及协议代码的过时注释 | `bridge-codex/src/`、`bridge-codex/tests/`（包含该 crate 自己的 fake）；`schemas/`、`fixtures/` | 不改应用端口、CLI 的 fake、Cargo 清单、xtask；schema 维护命令先在 PR 写清规格，由 D 提供入口或由 E 补接 |
| D / `build/tooling` | 第 3、15 项；第 2 项包校验；第 4 项 pip 清理；第 13 项边界门禁 | `xtask/`、`.cargo/`、`.github/`、`package.sh`、根 Cargo.toml/Cargo.lock 及 xtask 清单 | 本批不迁移假进程、不批量改测试、不移动生产文件、不修改生产 crate 清单、不裁剪已有共享 features 或改默认成员 |

第一批冻结接口为 `bridge-app` 的 `ports.rs`、`messaging.rs`、`requests.rs`、`events.rs`、`diagnostics.rs`，以及 sessions/directories 中供 adapter 实现的公共 trait。冻结其签名和契约；B 可整理同文件内部实现，但不能迫使 A、C 或 adapter 同时适配。`runtime::run` 的外部签名也保持不变，B 可以使用内部状态结构封装它。

A 的配对码规则在 CLI 层集中复用，保留当前应用/存储调用契约；如后续必须调整跨 crate 校验接口，列入 F，不能临时扩大第一批修改范围。B 的新增行为测试优先采用局部端口 fake；需要扩展 CLI 假进程的场景延后 E，已有 fake 协议在本批保持稳定。

D 可实现统一检查入口，但必须调用当前测试方式，不能把新的测试支持包作为前提。D 的 Cargo 修改限于自己的工具依赖和门禁；A–C 默认使用已有依赖。必要新增依赖由维护者安排单独的清单 PR，合并后仅依赖方同步该 commit，其他工作包无需等待。

### 后续工作包：主动串行化高冲突修改

| 工作包 / 分支 | 开始条件 | 任务与写入范围 |
| --- | --- | --- |
| E / `refactor/test-infrastructure` | A–D 已合入汇总分支，记录新基线 B1 | 第 11 项、第 13 项剩余清单与 features 整理；测试支持包、所有 Cargo 清单、假进程、现有测试的公共装配/路径迁移、xtask/CI 适配；补接协议维护入口。不得顺带重构业务实现 |
| F / `refactor/ports-diagnostics` | E 已合入汇总分支，记录新基线 B2 | 第 7、9 项和第 8 项剩余跨层类型/错误迁移；统一修改端口、runtime 调用点、local/Feishu adapter、CLI bootstrap/logging 及相关测试 |

将测试支持迁移放在 E，是为了避免正在重构 runtime 或协议的设备被迫跟随另一设备的 fake 路径变化；将跨层端口迁移放在 F，是为了避免每条分支独立发明不兼容接口。这两类修改不追求同时开工。E、F 必须从对应的新基线创建分支，不能在 B0 上长时间开发后一次性解决全部冲突。

其他设备在 E、F 期间可以只读审查、运行独立回归、复核文档或报告问题；如需写测试，先认领独立新文件并避开正在迁移的接口。每个工作包可以由任意设备上的独立 agent 承担，不要求继续使用上一批的 agent。

### 异步认领、越界与交付

1. 用户或维护者在远程 issue/PR 中为工作包登记唯一负责人、分支名、基线 SHA、写入范围和依赖。仅在各自本地编辑一份认领表不能防止重复领取；未被分配的 agent 不自行抢占工作包。
2. 领取者先 fetch 并确认基线，阅读本计划和归属文件，在自己的 clone 中创建工作分支。其他设备上的路径、临时目录、编译产物和未提交修改不可作为输入。
3. 需要修改非归属文件时，在 issue/PR 提出具体接口或补丁需求，写明原因、调用方和测试影响，等待维护者确认归属调整或前置 PR 合并。等待期间可以继续本范围内独立工作，不能替另一设备修改其分支，也不能通过复制接口绕过依赖。
4. 各 agent 在自己的 PR 描述中持续记录状态、已完成项和文档修改建议；不共同修改 `PLAN.md` 的勾选状态或往仓库增加多份相互矛盾的总计划。README、AGENTS.md 和现有 docs 由维护者通过独立文档 PR 更新，在相关代码批次合并后、最终验收前完成。文档任务另行分配时也遵守单文件归属。
5. 每个工作包交付可独立构建的 PR，目标为远程汇总分支。交付信息必须包含工作包编号、基线与最终 commit SHA、修改文件、接口兼容说明、测试环境/命令/结果、未完成项、关联依赖 PR，以及建议的文档修改。无 PR 工具时由用户转交同样的信息；不得假定其他 agent 能看到本次会话。

### 合并规则与独立测试

- A–D 可按完成和审核情况任意顺序合并，无固定“工具先完成”要求。某 PR 新增了真实依赖时，必须在 PR 明示，并先合并依赖 PR，不能靠临时 cherry-pick 把不同版本接口藏入分支。
- 合并前负责人将最新远程汇总分支 merge 到自己的分支，解决冲突并重测；默认不 rebase/强推已经共享的分支。没有文本冲突也必须检查接口与行为兼容。汇总分支更新后，旧 commit 的测试结果不自动证明新的合并结果通过。
- 维护者逐个合并 PR；每次至少检查构建与受影响测试，公共接口或测试基础设施变化运行全量检查。有合并队列时对实际合并候选运行 CI；没有时在最新汇总分支验证通过后再合下一个 PR。失败时暂停后续依赖合并，交由原负责人修复，不在主分支上堆积未解决问题。
- 禁止整文件选择 ours/theirs 消除冲突，也不允许为了通过测试删除另一分支的测试。无法独立解决的语义冲突在 PR 留下具体证据，由相关负责人和维护者异步确认。
- 每台设备使用 `rust-toolchain.toml` 指定的 Rust stable 工具链和对应 commit 的 Cargo.lock；记录 Linux 架构、stable 版本及工具链信息。非 Linux 设备使用独立 Linux 环境或远程 Linux CI，不把跳过平台测试记为通过。
- 构建缓存、临时目录和测试状态属于各自设备；不交换 target 产物或依赖本机绝对路径。普通测试不访问真实服务/凭据，所需依赖可预先获取，测试执行保持离线。旧打包脚本仍使用本地默认 target，自定义 target 场景由 D 修复后测试。
- 本地开发先运行定向回归；PR 标记可合并前执行当前完整验证命令。`cargo xtask check` 尚未合并时使用 README 的现有命令，落地后才能切换。D、E 另验证 release 包；F 验证所有端口调用方和旧持久状态兼容。
- A–D 合并后完整验证并记录 B1；E 合并后完整验证并记录 B2；F 和配套文档合并后，在最终汇总 commit 上完成一次完整验收。全部通过后由用户或维护者决定合入主分支，不自动部署。各设备分别通过的结果不能替代最终集成结果。

### 给独立设备上的 agent 的任务模板

> 领取 PLAN.md 工作包 `<A/B/C/D/E/F>`，分支 `<表中分支名>`，共同基线 `<远程可获取的完整 SHA>`，PR 目标 `integration/rust-remediation`。仅修改该工作包允许的文件，遵守冻结接口和依赖阶段；不要创建子 agent 或承担其他工作包。先确认基线可用，再开发并测试。需要越界修改时在 PR/交接信息中提出，未协调前不执行。交付最终 commit、测试证据、未完成项和文档建议，不修改共享计划状态、不自行合入主分支或部署。

## 验收与交接

完成标准：

- [x] 上述每项均有实现提交及对应验证结果；无法完成的项目保留为未完成，并写明具体原因。
- [x] 现有命令、审批、问答、Plan、目录、会话、附件与取消回归通过。
- [x] 新增 status、打包路径、包内文档、凭据变量、交互唯一实现和过载控制的回归通过。
- [x] 发布目录只包含产品二进制、示例配置、完整指南和构建校验信息。
- [x] README、架构说明、开发指南、部署指南、运维指南和 AGENTS.md 按最终实现同步更新；不得恢复失效迁移约束。只有确需记录新决策时才新增 ADR。
- [x] `PLAN.md` 保留此整改清单与实测结果，不再使用笼统“原生迁移已完成”替代验收证据。

按上节的工作包依赖合并，每批保持可构建、可测试，避免一次提交混入全部重构。由用户或维护者根据已经合并的提交和验证证据统一更新本清单；开发分支完成不等于集成完成。

真实飞书交互、目标主机生命周期和长时间稳定性单列为部署验收。本轮结构整改不擅自执行，也不将离线通过写成生产已验收。
